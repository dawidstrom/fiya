current_type = types.none;
is_pressed = false;
gui_shown = false;
locked = false; //true;

item_controller = instance_find(oItemController, 0);
spawner = instance_find(oSpawner, 0);
game_controller = instance_find(oGameController, 0);
drop_bar = instance_find(oDropBar, 0);
upgrade_controller = instance_find(oUpgradeController, 0);